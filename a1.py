def __init__(self):
        '''(Multiset) -> Nonetype
        Initialize the Multiset. The empty list forms to save the elements
        that will be added.
        '''
        SkipList.__init__(self)